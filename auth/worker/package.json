{
  "name": "bmr-patreon-auth-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/session.js && node --check src/patreon.js && node --check src/r2.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
